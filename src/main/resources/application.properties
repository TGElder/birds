spring.http.multipart.max-file-size=100MB
spring.http.multipart.max-request-size=100MB

spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=${TGELDER_BIRDS_DATASOURCE_URL}
spring.datasource.username=${TGELDER_BIRDS_DATASOURCE_USERNAME}
spring.datasource.password=${TGELDER_BIRDS_DATASOURCE_PASSWORD}

storage.location=upload-dir

api.username=${TGELDER_BIRDS_API_USERNAME}
api.password=${TGELDER_BIRDS_API_PASSWORD}
